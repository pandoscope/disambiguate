## Rule-code

The stable identifier of one [drift](drift.md) detection rule, e.g.
`unlinked-term`. Every drift finding names its rule-code in the output so
the finding can be silenced precisely rather than wholesale.
